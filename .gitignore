# Compiled gsettings schemas — build artifact, regenerated by `make schemas`
src/schemas/gschemas.compiled

# Packed extension bundles
*.shell-extension.zip

# Editor / OS noise
.DS_Store
*.swp
*~
.idea/
.vscode/

# Vendored agent skills — third-party docs pulled in by skills-lock.json,
# with their own provenance and machine-local paths. Tooling, not the
# extension; re-fetch rather than republish.
.agents/
.claude/
